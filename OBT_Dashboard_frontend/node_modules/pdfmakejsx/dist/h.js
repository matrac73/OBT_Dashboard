"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ramda_1 = require("ramda");
exports.h = function () {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
    }
    var tag = args[0], attributes = args[1], rest = args.slice(2);
    if (ramda_1.is(Function, tag)) {
        return tag(attributes, rest);
    }
    var children = rest || [];
    return {
        type: 'element',
        tagName: tag,
        attributes: attributes || {},
        children: ramda_1.flatten(children).map(function (d) { return d.type && d.type === 'element' ? d : { type: 'text', text: d }; })
    };
};
